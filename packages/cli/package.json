{
  "name": "@principal-ai/visual-validation-cli",
  "version": "0.1.0",
  "description": "Visual Validation CLI - Validate and manage .canvas configuration files",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "vv": "dist/index.js",
    "visual-validation": "dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "bun run clean && bun run build:lib && bun run build:cli",
    "build:lib": "tsc -p tsconfig.build.json",
    "build:cli": "bun build/build-cli.ts",
    "clean": "rm -rf dist",
    "dev": "tsx src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "visual-validation",
    "cli",
    "canvas",
    "graph",
    "validation",
    "principal-ai"
  ],
  "author": "Principal AI Team",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@principal-ai/visual-validation-core": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "globby": "^14.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "restricted",
    "registry": "https://registry.npmjs.org/"
  }
}
