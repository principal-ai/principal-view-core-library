[test]
# Use bun's built-in test runner with React Testing Library
preload = ["./src/**/*.test.tsx", "./src/**/*.test.ts"]
coverage = true

# Use happy-dom for DOM environment in tests
# This provides a lightweight DOM implementation for React component tests
